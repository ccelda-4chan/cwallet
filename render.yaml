services:
  - type: web
    name: nexus-wallet
    env: python
    startCommand: gunicorn -b 0.0.0.0:$PORT app:app
    envVars:
      - key: RENDER
        value: "true"
      - key: PYTHON_VERSION
        value: 3.10
